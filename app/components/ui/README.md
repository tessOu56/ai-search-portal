# components/ui — synced from explore-design-sdk

**Canonical（S2 起，2026-07-08）**：[explore-design-sdk `packages/components`](https://github.com/tessOu56/explore-design-sdk)。

本目錄為 vendored 副本（`@explore-design/components` 尚未發布 npm）。**改元件請改 SDK repo**，再同步回此處；發布後本目錄將移除、改為 `import { Button } from "@explore-design/components"`。

差異容忍：`~/shared/utils/cn` import 路徑為 portal 專屬（SDK 版為 `./cn`），其餘內容應與 SDK 逐字一致。
