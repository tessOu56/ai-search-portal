# Dataset v1

第一版 mock 情境；對應目前契約與 handler／store fixture。

- Items：fixture 目前仍寫在 `app/test/handlers.ts` 的 `itemsFixtureRaw`。
- Governance applications：`governance-applications.json`（T-2026-248）供 `access-request-store.server.ts` 載入。
- 見 `docs/conventions/mock-dataset-versioning.md`。
