import{_ as e,o as t,c as s,ae as i}from"./chunks/framework.BX6FPmO9.js";const u=JSON.parse('{"title":"資料視覺化規劃（Data Viz Plan）","description":"","frontmatter":{},"headers":[],"relativePath":"DATA-VIZ-PLAN.md","filePath":"DATA-VIZ-PLAN.md"}'),o={name:"DATA-VIZ-PLAN.md"};function l(n,a,d,r,c,h){return t(),s("div",null,[...a[0]||(a[0]=[i(`<h1 id="資料視覺化規劃-data-viz-plan" tabindex="-1">資料視覺化規劃（Data Viz Plan） <a class="header-anchor" href="#資料視覺化規劃-data-viz-plan" aria-label="Permalink to &quot;資料視覺化規劃（Data Viz Plan）&quot;">​</a></h1><blockquote><p>2026-07-07 初版。目標：讓用戶在 portal 內以圖表檢視 catalog / 搜尋資料，並提供圖表類型選項。</p></blockquote><h2 id="目標" tabindex="-1">目標 <a class="header-anchor" href="#目標" aria-label="Permalink to &quot;目標&quot;">​</a></h2><ol><li>新增 <code>/insights</code> 頁：用戶可視覺化檢視資料（項目分布、搜尋趨勢、類型佔比）。</li><li>提供圖表選項：bar / line / pie 可切換，之後擴充 area、table 檢視。</li><li>架構上與既有 feature 模式一致（<code>app/features/insights/</code>），可漸進接上真實資料。</li></ol><h2 id="選型" tabindex="-1">選型 <a class="header-anchor" href="#選型" aria-label="Permalink to &quot;選型&quot;">​</a></h2><table tabindex="0"><thead><tr><th>候選</th><th>評估</th><th>結論</th></tr></thead><tbody><tr><td><strong>recharts</strong></td><td>React 原生宣告式、與 nx-playground 生態一致、SSR 需 ClientOnly 包裝</td><td>✅ 採用 <code>^3.8.1</code></td></tr><tr><td>chart.js + react-chartjs-2</td><td>canvas 效能好，但命令式 config、與 Tailwind 主題整合較繞</td><td>備選</td></tr><tr><td>visx</td><td>最靈活但要自組，POC 成本高</td><td>不採用</td></tr><tr><td>ECharts</td><td>功能最全但 bundle 大，portal 用不到地圖/3D</td><td>不採用</td></tr></tbody></table><p>Flow/節點圖需求（如 agent pipeline 視覺化）另用 <code>@xyflow/react</code>，不混入本規劃；先在 nx-playground event-cms 驗證後再評估移植。</p><h2 id="資訊架構" tabindex="-1">資訊架構 <a class="header-anchor" href="#資訊架構" aria-label="Permalink to &quot;資訊架構&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app/routes/insights._index.tsx      # /insights 路由（meta + loader）</span></span>
<span class="line"><span>app/features/insights/</span></span>
<span class="line"><span>  ├── InsightsPanel.tsx             # 主面板：圖表類型切換 + 圖表渲染</span></span>
<span class="line"><span>  ├── insights.mock.ts              # Phase 1 mock 聚合資料</span></span>
<span class="line"><span>  └── index.ts</span></span></code></pre></div><ul><li>圖表在 <code>ClientOnly</code>（remix-utils）內渲染，避免 SSR window 依賴。</li><li>圖表類型狀態放 URL searchParam（<code>?chart=bar</code>）以利分享連結（POC 先用 useState，Phase 2 收斂到 URL）。</li></ul><h2 id="資料流-分期" tabindex="-1">資料流（分期） <a class="header-anchor" href="#資料流-分期" aria-label="Permalink to &quot;資料流（分期）&quot;">​</a></h2><ul><li><strong>Phase 1（本次 POC）</strong>：mock 聚合資料（<code>insights.mock.ts</code>），驗證 UI 與切換體驗。</li><li><strong>Phase 2</strong>：loader 端聚合真實資料 — 復用 <code>catalog-search.server.ts</code> 資料源，輸出 <code>{ label, value }[]</code> 契約，型別進 <code>packages/shared-contracts</code>。</li><li><strong>Phase 3</strong>：後端 metrics API（<code>api.insights.ts</code>），支援時間區間 / 類型篩選參數；快取於 loader。</li></ul><h2 id="待辦-phase-2-起" tabindex="-1">待辦（Phase 2 起） <a class="header-anchor" href="#待辦-phase-2-起" aria-label="Permalink to &quot;待辦（Phase 2 起）&quot;">​</a></h2><ul><li>[ ] i18n：<code>insights.*</code> 翻譯鍵（zh-TW / en），套 <code>getTranslations</code> 既有模式</li><li>[ ] SEO：<code>buildSeoMeta</code> + JSON-LD（比照 catalog-search）</li><li>[ ] 導覽入口：sidebar / 首頁加 <code>/insights</code> 連結</li><li>[ ] 測試：InsightsPanel smoke test（比照 <code>catalog-search.smoke.test.tsx</code>）</li><li>[ ] a11y：圖表加 <code>role=&quot;img&quot;</code> + 摘要文字（eslint-plugin-jsx-a11y 已啟用）</li></ul>`,14)])])}const g=e(o,[["render",l]]);export{u as __pageData,g as default};
