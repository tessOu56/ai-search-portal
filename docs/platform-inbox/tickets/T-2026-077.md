---
id: T-2026-077
project: ai-search-portal
horizon: short
priority: P0
status: in_progress
executor: cloud
issueUrl: null
distributedAt: 2026-07-14
---

# Cloud-agent loop pilot — ship homepage motion + glass scrollbar; close acceptance feedback

## Spec refs (in-repo)

- `app/routes/_index.tsx`
- `app/components/app/home/HomeLanding.tsx` (create if missing)
- `app/components/app/workspace/WorkspaceChatView.tsx`
- `app/tailwind.css` (global scrollbar)
- `app/components/ui/ScrollArea.tsx`
- `app/styles/scale.system.css` (if present; else extend Tailwind spacing/type as needed)
- `docs/runbooks/deployment.md` (cold-start / LCP notes only if already touching docs)

Orchestrator-only SSOT (do **not** require reading these to implement): platform-command ticket yaml / cloud-agent-ticket-flow.

## Acceptance

- [ ] Read this file under `docs/platform-inbox/tickets/T-2026-077.md` (self-contained; no platform-command access required)
- [ ] Ship or reconcile **HomeLanding**: full-bleed atmosphere, hero brand, one tagline, one CTA group (Ask LUI + dashboard); motion enter + bg drift; `prefers-reduced-motion` respect; **no hero cards / badge wall / stats**
- [ ] Ship or reconcile **glass theme scrollbar**: thin translucent glass track + accent thumb (global WebKit/Firefox + Radix `ScrollArea`); **no** native arrows / button icons / masks
- [ ] Wire `_index` shell: floating view switcher; chat content below landing (`#home-chat`); dashboard via `?view=dashboard`
- [ ] Add `motion` dependency if missing; hero asset under `public/marketing/home-atmosphere.*` (webp/svg/jpg, keep modest size)
- [ ] `pnpm run pr-gate` green; `feature/*` branch + PR to `main` (Conventional Commits)
- [ ] **(human)** Visual QA on `/` and Chat ScrollArea (蜜蝋 / 若草); Outcomes + ticket `done` + redistribute — **Cloud skip**
- [ ] **(human)** Pages Settings / `VERCEL_*` secrets — **Cloud skip**

## Scope notes

| Area            | Paths                                                                                                                     |
| --------------- | ------------------------------------------------------------------------------------------------------------------------- |
| Landing         | `app/components/app/home/HomeLanding.tsx`, `app/routes/_index.tsx`                                                        |
| Chat layer      | `app/components/app/workspace/WorkspaceChatView.tsx` (slim copy; no badge/card wall)                                      |
| Scroll          | `app/tailwind.css`, `app/components/ui/ScrollArea.tsx`                                                                    |
| Optional chrome | `StarCursor` / `NavProgress` if already started locally — do not block PR if out of scope; prioritize landing + scrollbar |

Design tokens: existing portal theme (蜜蝋 / 若草). No purple AI-gradient default look.

## Cloud agent

executor: cloud

Paste into Cloud Agent (product repo only):

```text
Repo: ai-search-portal only. Ticket: T-2026-077.

Read: AGENTS.md -> docs/PROJECT-PLAN.md -> docs/platform-inbox/CURRENT.md
-> docs/platform-inbox/tickets/T-2026-077.md

Implement only this ticket's acceptance (skip lines marked human).
Done when: pnpm run pr-gate green, feature/* + PR to main, Conventional Commits.

Constraints: no Vercel Git auto-deploy; no force-push; no --no-verify.
```

## Notes

_Received from platform-command distribute-inbox. Ticket body is SSOT for Cloud; push of this file to origin/main is required before Cloud can see it._
