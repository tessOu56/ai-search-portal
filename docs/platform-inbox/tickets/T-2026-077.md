---
id: T-2026-077
project: ai-search-portal
horizon: short
priority: P0
status: done
executor: local
issueUrl: null
distributedAt: 2026-07-16
---

# Cloud-agent loop pilot — ship homepage motion + glass scrollbar; close acceptance feedback

## Spec refs (in-repo)

- `app/routes/_index.tsx`
- `app/components/app/home/HomeLanding.tsx`
- `app/components/app/workspace/WorkspaceChatView.tsx`
- `app/tailwind.css` (global scrollbar)
- `app/components/ui/ScrollArea.tsx`
- `app/styles/scale.system.css` (if present)
- `public/marketing/home-atmosphere.*`

Orchestrator-only SSOT (do **not** require reading to implement): platform-command ticket yaml / cloud-agent-ticket-flow.

## Acceptance

- [x] Read this file under `docs/platform-inbox/tickets/T-2026-077.md` (self-contained; no platform-command access required)
- [x] Ship or reconcile **HomeLanding**: full-bleed atmosphere, hero brand, one tagline, one CTA group; motion; no hero cards / badge wall
- [x] Ship or reconcile **glass theme scrollbar**: thin translucent track + accent thumb (global + Radix ScrollArea); no native arrows/icons
- [x] Wire `_index` shell: floating view switcher; chat below landing; `?view=dashboard`
- [x] `pnpm run pr-gate` green; feature branch + PR to `main` (reconciled — code on `main`, e.g. `25a3e1d` era)
- [x] **(human)** Visual QA + outcomes + ticket done + redistribute — local reconcile 2026-07-16
- [ ] **(human)** Pages Settings / `VERCEL_*` secrets — Cloud skip (T-079 / T-067)

## Scope notes

| Area          | Paths                                                              |
| ------------- | ------------------------------------------------------------------ |
| Landing       | `app/components/app/home/HomeLanding.tsx`, `app/routes/_index.tsx` |
| Chat layer    | `app/components/app/workspace/WorkspaceChatView.tsx`               |
| Scroll        | `app/tailwind.css`, `app/components/ui/ScrollArea.tsx`             |
| Assets / deps | `public/marketing/home-atmosphere.*`, `motion` package             |

Design tokens: portal themes (蜜蝋 / 若草). No purple AI-gradient default.

## Reconcile evidence (2026-07-16)

| Item            | Evidence on `main`                                      |
| --------------- | ------------------------------------------------------- |
| HomeLanding     | BrandMark hero, motion, ask panel, no hero cards        |
| Shell           | `_index.tsx` mounts HomeLanding + WorkspaceViewSwitcher |
| Glass scrollbar | `tailwind.css` glass rail; `ScrollArea.tsx` Radix thumb |
| Dashboard       | `?view=` / view switcher                                |

## Cloud agent

executor was `cloud` for the pilot; coding acceptance **done** on main. Optional re-read only — do not re-implement unless regression.

```text
Repo: ai-search-portal only. Ticket: T-2026-077 (status: done).

git fetch origin && git checkout main && git pull

Read: docs/platform-inbox/tickets/T-2026-077.md
Coding acceptance already satisfied on main. Skip unless verifying regression.
```

## Notes

_Unblock lesson: distribute-inbox alone is not enough — commit+push product `docs/platform-inbox/` to origin/main before Cloud kickoff._
