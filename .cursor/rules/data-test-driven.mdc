---
description: Data-test-driven development — Contract-first, MSW, schema validation, enforcement
alwaysApply: true
---

# Data-Test-Driven Development (大型協作制度)

## 流程：Spec → Contract → Mock → Test → UI
1. **Feature Spec** — 業務、資料流、驗收（RA- / ticket）。
2. **Contract** — Zod schema 於 `app/shared/contracts/`；request/response 可 runtime 驗證。
3. **Mock** — MSW handler 在 `app/test/handlers.ts`；回傳須經契約 schema parse。
4. **Test** — 依 handler 與 fixture 撰寫；可先於 UI。
5. **UI** — 僅透過 useFetcher 或 shared api 打契約路徑，禁止 component 內直接 fetch 硬編碼 URL。

## 強制規則
- **Schema 可驗證**：使用 Zod，禁止僅依 TypeScript 型別。見 `specs/api/contract-schema.md`。
- **Handler = Contract**：每個 API 必有 MSW handler；handler 產出須經 schema parse。見 `specs/api/handler-mapping.md`。
- **API 走約定路徑**：禁止在 component 內直接 `fetch(url)`；用 useFetcher 或 `app/shared/api`。見 `app/shared/api/README.md`。
- **CI**：`npm run lint:handlers` 檢查 api route 是否有對應 handler；未通過則 PR 失敗。

## 適用範圍
- 新增／變更「對外 API」→ 必須走完整流程。
- 僅改 UI 樣式／文案、不碰 API → 不需 Contract/Mock。
- 見 `docs/conventions/data-test-driven.md` 完整制度與漂移策略。

## 資料治理與 Mock 版本
- 前端對等「可治理資料層」：Contract-first、MSW 即規格、Mock Dataset 版本化（可回溯、可重現）。見 `docs/architecture/data-governance.md`、`docs/conventions/mock-dataset-versioning.md`。優化順序：先 contract + service + CI，再視需求加 query cache / BFF。
