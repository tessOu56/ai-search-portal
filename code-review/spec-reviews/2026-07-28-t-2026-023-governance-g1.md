# Spec review — T-2026-023 Governance demo flow (G1)

| Field    | Value                |
| -------- | -------------------- |
| Ticket   | T-2026-023           |
| Date     | 2026-07-28           |
| Reviewer | coding-agent         |
| Verdict  | **ready-with-notes** |

## 1. Scope

Governance demo shell: api-detail (datacontract fields / PII / owner / terms), apply lifecycle, my-apis requester tracking, owner/admin pending review, mock session role switch. Contracts only via `@ai-search-portal/contracts`.

## 2. Upstream specs read

- `specs/domain/metadata-access.yaml`
- `specs/datacontracts/customer-profile.yaml`
- `specs/policies/access-request.rego` (+ `_test.rego`)
- `specs/api/handler-mapping.md` (access-requests POST / evaluate)
- `packages/shared-contracts` access-request + metadata contracts
- `labs/design-vibe/GAP-REPORT.md` (api-detail / my-apis)
- `docs/product/ai-experience-plan.md` / `interface-roadmap.md`

## 3. Drift findings

| Area            | Finding                                                                                                               | Resolution for G1                                                                                                        |
| --------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| Lifecycle names | Ticket: `draft→submitted→approved/rejected→expired`. Domain: `draft\|pending_approval\|approved\|denied` (no expired) | **Domain remains SSOT.** Treat ticket `submitted`≡`pending_approval`, `rejected`≡`denied`. Add `expired` to domain yaml. |
| Persistence     | POST `/api/metadata/access-requests` returns status but does not store applications                                   | Add in-memory store + GET list + review mutation                                                                         |
| Terms           | `customer-profile.yaml` has `termsOfUse`; metadata detail / Zod detail omit it                                        | Optional `termsOfUse` on detail contract; hydrate for known assets                                                       |
| Session roles   | Ticket: requester/owner/admin. Domain: analyst/data_admin/engineer (policy)                                           | Keep policy roles; add separate **session persona** for UI mock switch                                                   |
| OpenAPI         | Access-request list/review not in OpenAPI                                                                             | Zod-first (repo ADR); document in handler-mapping; OpenAPI follow-up optional (not blocking G1)                          |
| GAP Figma       | api-detail / my-apis Figma nodes TBD (STOP-003)                                                                       | Waiver; mock-first shell acceptable                                                                                      |

## 4. Gaps to close in implementation (this ticket)

1. Persist applications with lifecycle + permissionStatus.
2. Detail surface: owner, PII/classification, columns/sensitive, **termsOfUse**.
3. Routes: `/my-apis` (requester), `/access-requests/review` (owner/admin approve/deny).
4. Mock session persona switch (`?sessionRole=` or cookie); loading/empty/error/success states.
5. MSW + handler-mapping + tests for list/review.

## 5. Impacted files (plan)

| Layer     | Files                                                                                                  |
| --------- | ------------------------------------------------------------------------------------------------------ |
| Spec      | `specs/domain/metadata-access.yaml`, `specs/api/handler-mapping.md`, GAP-REPORT note                   |
| Contract  | `packages/shared-contracts/src/access-request.contract.ts`, `metadata.contract.ts`, `index.ts`         |
| Service   | `app/services/access-request-store.server.ts`, extend `access-policy.server.ts`                        |
| API       | GET on `api.metadata.access-requests`, `api.metadata.access-requests.$requestId.review`                |
| UI        | `MetadataAssetDetailView`, `app/features/accessrequests/*`, routes `my-apis`, `access-requests.review` |
| Mock/test | `metadata-handlers.ts`, integration + smoke tests                                                      |

## 6. Non-goals (this PR / G1)

- Real OPA remote; HITL server enforcement beyond mock (T-068)
- G2 realistic seed expansion (T-024)
- RESUME-DEMO script (T-025)
- OpenAPI codegen upgrade for new list/review paths (optional follow-up)
- Figma pixel alignment (STOP-003)

## 7. Breaking change?

No — additive fields/endpoints; existing POST submit response shape retained (extended persistence side-effect only).

## 8. Spec impact summary (for PR)

- Domain: add `expired`; document ticket↔domain aliases; add session personas.
- Contracts: application record + list/review schemas; optional `termsOfUse` on metadata detail.
- APIs: GET `/api/metadata/access-requests`; POST `…/:id/review`.
- UI: my-apis + review shell; detail terms.

## 9. Verdict notes

**ready-with-notes** — may implement once domain yaml aliases + expired are written in the same change set as contracts. Do not invent parallel status enums in UI.
